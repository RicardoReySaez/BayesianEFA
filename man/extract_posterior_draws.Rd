% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_posterior.R
\name{extract_posterior_draws}
\alias{extract_posterior_draws}
\title{Extract Posterior Draws}
\usage{
extract_posterior_draws(
  befa_fit,
  pars,
  format = c("matrix", "array", "df", "list", "rvars")
)
}
\arguments{
\item{befa_fit}{A \code{befa} object.}

\item{pars}{Character vector. Parameter names to filter (passed to \code{\link[posterior:subset_draws]{posterior::subset_draws()}}).}

\item{format}{Character. Output format: "matrix", "array", "df", "list", or "rvars".}
}
\value{
The posterior draws in the requested format.
}
\description{
A wrapper around the \code{posterior} package to extract draws from a \code{befa}
object in various formats.
}
\examples{
\dontrun{
befa_fit <- befa(
  data = HS_data, n_factors = 3, model = "cor",
  iter = 500, chains = 2, seed = 123
)

# Extract Lambda draws as a matrix
lambda_draws <- extract_posterior_draws(befa_fit, pars = "Lambda", format = "matrix")

# Extract as rvars for easy summaries
lambda_rvars <- extract_posterior_draws(befa_fit, pars = "Lambda", format = "rvars")
}

}
