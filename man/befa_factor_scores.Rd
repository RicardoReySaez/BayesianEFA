% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/befa_factor_scores.R
\name{befa_factor_scores}
\alias{befa_factor_scores}
\title{Bayesian Factor Scores}
\usage{
befa_factor_scores(object, post_summaries = TRUE)
}
\arguments{
\item{object}{A \code{befa} object returned by \code{\link[=befa]{befa()}}.}

\item{post_summaries}{Logical. If \code{TRUE} (default), includes posterior summaries
computed via \code{\link[posterior:draws_summary]{posterior::summarise_draws()}}. Set to \code{FALSE} to skip summaries.}
}
\value{
A list of class \code{befa_factor_scores} containing:
\itemize{
\item \code{eta_draws}: A \code{draws_array} (iterations \eqn{\times} chains \eqn{\times} P)
containing the posterior draws of the factor scores.
\item \code{summary}: (If \code{post_summaries = TRUE}) A \code{draws_summary} data frame computed
via \code{\link[posterior:draws_summary]{posterior::summarise_draws()}}.
}
}
\description{
Computes posterior factor scores from a fitted BEFA model by evaluating the
conditional posterior distribution of the latent factors given the observed data
and the estimated model parameters.
}
\details{
\strong{Factor Score Estimation}

Consistent with the data generation process of the factor model, the observation
for the \eqn{i}-th individual is defined as:
\deqn{\large \mathbf{y}_i = \boldsymbol{\nu} + \mathbf{\Lambda} \cdot \boldsymbol{\eta}_i + \boldsymbol{\epsilon}_i}

where \eqn{\boldsymbol{\eta}_i \sim \mathcal{N}(\mathbf{0}, \mathbf{I}_M)} and
\eqn{\boldsymbol{\epsilon}_i \sim \mathcal{N}(\mathbf{0}, \mathbf{\Psi})}.

Because the posterior distributions of the model parameters are available from
the MCMC draws, the factor scores can be derived analytically for each iteration.
The conditional posterior distribution of the latent factors given the observed data is:
\deqn{\large \boldsymbol{\eta}_i \mid \mathbf{y}_i, \mathbf{\Lambda}, \mathbf{\Psi}, \boldsymbol{\nu} \sim \mathcal{N}(\boldsymbol{\mu}_{\eta \mid y}, \mathbf{\Sigma}_{\eta \mid y})}

with the conditional covariance matrix and mean vector defined as:
\deqn{\large \mathbf{\Sigma}_{\eta \mid y} = (\mathbf{I}_M + \mathbf{\Lambda}^\top \cdot \mathbf{\Psi}^{-1} \cdot \mathbf{\Lambda})^{-1}}
\deqn{\large \boldsymbol{\mu}_{\eta \mid y} = \mathbf{\Sigma}_{\eta \mid y} \cdot \mathbf{\Lambda}^\top \cdot \mathbf{\Psi}^{-1} \cdot (\mathbf{y}_i - \boldsymbol{\nu})}

\strong{Handling of Missing Data}

For incomplete observations, the estimation naturally adapts by conditioning
only on the observed subset of variables for person \eqn{i}. Specifically, the
matrices \eqn{\mathbf{\Lambda}} and \eqn{\mathbf{\Psi}}, as well as the
data vector \eqn{(\mathbf{y}_i - \boldsymbol{\nu})}, are subsetted to the
indices of the observed variables, denoted as \eqn{o_i}.
}
\examples{
\dontrun{
# ─────────────────────────────────────────────────────── #
#    1. Factor scores after fitting Bayesian EFA model    #
# ─────────────────────────────────────────────────────── #

# Fit Bayesian EFA model to the famous Grant-White School Data (Holzinger y Swineford , 1939)
befa_fit <- befa(
  data = HS_data,
  n_factors = 3,
  factor_scores = FALSE,
  compute_fit_indices = FALSE,
  compute_reliability = FALSE,
  backend = "cmdstanr",
  seed = 8
)

# Compute factor scores from a fitted befa object
Bayes_fsc <- befa_factor_scores(befa_fit, post_summaries = TRUE)

# Store Posterior Means in a 300 x 2 Matrix
Bayes_fsc_pmean <- matrix(Bayes_fsc$summary$mean, nrow = nrow(HS_data), ncol = 3)

# Estimate frequentist EFA and store factor scores
efa_fit <- factanal(x = HS_data, factors = 3, scores = "regression")

# Equivalence between factor scores (we order the columns)
cor(efa_fit$scores[, 2], Bayes_fsc_pmean[, 1])
cor(efa_fit$scores[, 1], Bayes_fsc_pmean[, 2])
cor(efa_fit$scores[, 3], Bayes_fsc_pmean[, 3])

# Bivariate plot to see linear equivalence
par(mfrow = c(1, 3))
plot(Bayes_fsc_pmean[, 1], efa_fit$scores[, 2],
  main = "Factor 1",
  xlab = "Bayesian factor scores",
  ylab = "Frequentist factor scores"
)
abline(a = 0, b = 1, col = "steelblue2", lwd = 2)
plot(Bayes_fsc_pmean[, 2], efa_fit$scores[, 1],
  main = "Factor 2",
  xlab = "Bayesian factor scores",
  ylab = "Frequentist factor scores"
)
abline(a = 0, b = 1, col = "salmon2", lwd = 2)
plot(Bayes_fsc_pmean[, 3], efa_fit$scores[, 3],
  main = "Factor 3",
  xlab = "Bayesian factor scores",
  ylab = "Frequentist factor scores"
)
abline(a = 0, b = 1, col = "firebrick4", lwd = 2)
par(mfrow = c(1, 1))

# ─────────────────────────────────────────────────────────────────────────────
# ────────────────────────────────────────────────────── #
#    2. Factor scores when fitting Bayesian EFA model    #
# ────────────────────────────────────────────────────── #
# Fit Bayesian model with Factor Scores = TRUE
befa_fit <- befa(
  data = HS_data,
  n_factors = 3,
  factor_scores = TRUE,
  compute_fit_indices = FALSE,
  compute_reliability = FALSE,
  backend = "cmdstanr",
  seed = 8
)

# Factor scores (eta) are stored inside the befa object
extract_posterior_draws(befa_fit, pars = "eta")

# # A draws_matrix: 1000 iterations, 4 chains, and 903 variables
#     variable
# draw eta[1,1] eta[2,1] eta[3,1] eta[4,1] eta[5,1] eta[6,1] eta[7,1] eta[8,1]
#   1     -1.06    1.197    0.025    0.187     0.62     1.55    -1.65  -0.9944
#   2     -0.55    1.721    0.195    0.497     0.29     1.81    -0.87  -0.6410
#   3     -0.59    0.714   -0.510   -0.062    -0.60     0.39    -1.30  -0.1107
#   4     -1.48    0.863    0.333    0.737    -1.04    -0.20    -2.12  -0.0072
#   5     -1.01    1.230   -0.462    0.506    -1.27    -0.19    -1.96  -0.2326
#   6     -1.47    0.269    0.235    0.754    -1.43     1.15    -2.15   0.0757
#   7     -1.75   -0.028   -0.620    1.409    -1.05     0.44    -0.93  -0.5567
#   8     -1.00    0.750   -0.016   -0.013    -0.57    -0.26    -0.90  -0.1160
#   9     -0.16    0.968   -1.193    1.203    -1.18    -0.61    -1.79  -1.1739
#   10     0.70    0.740    0.662    0.361     0.32     0.49    -1.19   0.0822
# # ... with 3990 more draws, and 895 more variables

posterior_summaries(befa_fit, pars = "eta")

# # A tibble: 903 × 10
#    variable    mean median    sd   mad     q5    q95  rhat ess_bulk ess_tail
#    <chr>      <dbl>  <dbl> <dbl> <dbl>  <dbl>  <dbl> <dbl>    <dbl>    <dbl>
#  1 eta[1,1]  -0.750 -0.750 0.633 0.635 -1.79   0.281 1.00     3978.    3997.
#  2 eta[2,1]   0.604  0.612 0.610 0.588 -0.426  1.60  1.00     3849.    3599.
#  3 eta[3,1]  -0.177 -0.180 0.597 0.588 -1.16   0.812 1.000    3670.    3731.
#  4 eta[4,1]   0.714  0.717 0.585 0.594 -0.235  1.68  1.00     4190.    4058.
#  5 eta[5,1]  -0.521 -0.524 0.607 0.608 -1.53   0.476 1.000    3842.    3813.
#  6 eta[6,1]   0.461  0.465 0.622 0.612 -0.553  1.49  1.000    4063.    3953.
#  7 eta[7,1]  -1.40  -1.40  0.585 0.574 -2.36  -0.452 1.000    3952.    3999.
#  8 eta[8,1]  -0.117 -0.116 0.597 0.595 -1.09   0.869 1.00     3864.    3774.
#  9 eta[9,1]  -0.283 -0.273 0.613 0.611 -1.31   0.696 1.000    4115.    4014.
# 10 eta[10,1] -1.34  -1.34  0.587 0.572 -2.30  -0.359 1.00     4112.    3936.
# # ℹ 893 more rows
# # ℹ Use `print(n = ...)` to see more rows

# You can store posterior means easely because Stan always use column-major order
Bayes_fsc <- matrix(
  posterior_summaries(befa_fit, pars = "eta")$mean,
  nrow = nrow(HS_data),
  ncol = 3
)

# ─────────────────────────────────────────────────────────────────────────────
}

}
\references{
Thomson, G. H. (1951). \emph{The Factorial Analysis of Human Ability}. London University Press.
}
