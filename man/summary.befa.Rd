% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary_befa.R
\name{summary.befa}
\alias{summary.befa}
\title{Summary and Print Methods for BEFA Objects}
\usage{
\method{summary}{befa}(
  object,
  probs = c(0.025, 0.975),
  cutoff = 0,
  sort = FALSE,
  signif_stars = FALSE,
  ...
)
}
\arguments{
\item{object}{A \code{befa} object returned by \code{\link[=befa]{befa()}}.}

\item{probs}{Numeric vector of length 2. Quantiles for credible intervals (default: 0.025, 0.975).}

\item{cutoff}{Numeric. Loadings with absolute value below this threshold are hidden in print output.}

\item{sort}{Logical. If TRUE, items are sorted by their primary factor loading.}

\item{signif_stars}{Logical. If TRUE, marks loadings whose CI excludes zero with an asterisk.}

\item{...}{Ignored.}
}
\value{
A \code{summary.befa} object containing:
\itemize{
\item \code{estimates}: Data frame with loadings, h2, u2, and diagnostics per item.
\item \code{sig_matrix}: Logical matrix indicating which loadings have CIs excluding zero.
\item \code{probs}: The quantile probabilities used for credible intervals.
\item \code{phi}: Factor correlation matrix (identity for orthogonal rotations).
\item \code{variance}: List with per-factor and total explained variance proportions.
\item \code{fit_indices}: Fit measures from the \code{befa} object (if available).
\item \code{reliability}: Reliability estimates from the \code{befa} object (if available).
\item \code{header}: List with model metadata (model_type, n_factors, N, rotation).
\item \code{print_options}: List of display options (cutoff, sort, signif_stars).
\item \code{tables}: Pre-built display tables for loadings, fit measures, and reliability.
}
}
\description{
Methods to summarize and display results from Bayesian Exploratory Factor Analysis.

Computes posterior summaries for a fitted Bayesian EFA model, including
point estimates, credible intervals, and convergence diagnostics for factor loadings.
}
\details{
The summary extracts posterior draws for loadings (Lambda), computes means and quantiles,
and reports MCMC diagnostics (Rhat, ESS). Communalities (h2) and uniquenesses (u2)
are derived from the squared loadings.
}
\examples{
\dontrun{
befa_fit <- befa(
  data = HS_data, n_factors = 3, model = "cor",
  iter = 500, chains = 2, seed = 123
)

# Get and print the summary
befa_summary <- summary(befa_fit, sort = TRUE, signif_stars = TRUE)
print(befa_summary)
}

}
